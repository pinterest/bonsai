/*
 * @flow
 */

import React from 'react';

type Props = {};

export default function HowTo(props: Props) {
  return (
    <div>
      <p>
        Start by uploading a stats.json file. You can Drag+Drop or click Upload
      </p>
    </div>
  );
}
